# Retrospective — Epic 5: QA, Golden Tooling & Regression

Date: 2026-09-06
Stories: 5.1–5.4 done (4/4)
Suite close-out: 295 passed / 2 skipped (zhvi pytest)
Next epic: không có — hết roadmap epics.md

## Tóm tắt

Epic 5 khóa vòng đời VietPhrase-only bằng 3 lớp regression không model (QA structural, golden, metamorphic/dict unit) và một lệnh CLI chạy hết LEARN→TRANSLATE→export. Global auto-promotion vẫn fail-closed (AD-14).

## Thành công

- QA document-level chặn export, không hidden repair (AD-10). leftover_cjk nâng từ warning lên gate — lộ 5 test cũ, sửa bằng QualityOverrides (đúng quyền).
- Golden: `expect*` không tự thành approved; 4.3 đọc cùng `approved_cases`.
- Metamorphic 6 + dictionary unit 6, fixture `tmp_path`, 12 test trong < 1s.
- 5.4 quyết đúng `--no-learn` (thêm flag, không map lệnh). LEARN ở CLI, không trong `translate_project` (AD-4).
- Review 2 trục lặp lại trên 5.1/5.2/5.4; 5.3 isolation worktree rồi merge.

## Khó khăn / pattern

- Review vòng 1 luôn bắt provenance/hash/test tautology — lặp 5.1 và 5.2 (8+7 fix). Story context phải ghi schema field bắt buộc, không để dev đoán.
- leftover_cjk + production dict: mini fixture thiếu CJK cover thì E2E/QA fail. 5.4 dùng production dict → mỗi translate-learn ~1–2 phút, full suite ~10 phút (trước ~8).
- `_mini_dict_dir` copy ở nhiều test file — không block, nhưng dễ lệch cover.
- Story markdown `_bmad-output/implementation-artifacts/*.md` untracked suốt epic 3–5.

## Bài học

1. Gate fail-closed cần test âm (unapproved không mở global; `--no-learn` observation = 0), không chỉ happy path.
2. CLI contract (`translate -p --no-learn` không FILE) phải được story nói rõ; pipeline `source is None` fail — CLI phải resolve snapshot.
3. Isolation worktree (5.3) tránh đụng `pipeline.py` khi 5.1 đang sửa — giữ pattern khi song song.

## Follow-through retro trước

Không có file `epic-*-retro-*.md`. Epic 1–4 retrospective vẫn `optional`. Đây là retro đầu tiên được ghi.

## Epic tiếp theo

Không có Epic 6 trong epics.md. Việc còn lại ngoài epic 5:

- Epic 3: mọi story `done` nhưng `epic-3: in-progress` — hygiene sprint-status.
- Epic 4: `4.3` vẫn `review`; epic-4 chưa đóng.
- AD-14: global auto-promotion khóa tới khi có golden manifest approved thật (người duyệt `raw_china`).

## Action items

1. Gắn `pytest.mark.slow` (hoặc mini-dict) cho E2E `test_cli_e2e.py` happy-path có LEARN — suite local/CI không bị ~2 phút/test. Owner: Amelia (Dev). Status: open.
2. Quyết commit hoặc gitignore story markdown `_bmad-output/implementation-artifacts/[3-5]-*.md`. Owner: Falcol. Status: open.
3. Đóng 4.3 (review → done) rồi `epic-4: done`; đánh `epic-3: done` vì 3.1–3.5 đã done. Owner: Falcol. Status: open.

## Readiness

- Testing: metamorphic/dict unit nhanh; E2E CLI có LEARN chậm nhưng pass.
- Deployment: CLI local, không production deploy.
- Stakeholder: README đã mô tả 2 pha + AD-14 khóa.
- Codebase: ổn định cho vận hành book-level; global-auto vẫn khóa có chủ ý.

## Commitments

- Không bật `zhvi learn --global` promote cho đến golden approved.
- Story mới: ghi CLI contract exact (có/không FILE) và test âm cho skip-path.
