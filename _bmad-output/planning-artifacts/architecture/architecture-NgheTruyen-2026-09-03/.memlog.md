---
goal: Tái thiết kế zhvi thành hệ thống dịch thuần VietPhrase và tự nâng cấp từ điển
source: thiet-ke-he-thong-dich-truyen-zh-vi.md
mode: fast-path
date: 2026-09-03
updated: 2026-09-03T23:17
---

- (direction) Người dùng chọn Fast path; viết toàn bộ spine và đánh dấu giả định cần benchmark.
- (constraint) Không dùng HachimiMT, Qwen, Ollama hoặc bất kỳ model AI nào để dịch, hậu kiểm hay chỉnh văn bản.
- (constraint) VietPhrase là engine dịch duy nhất; kết quả phải xác định và tái lập từ source revision + dictionary revision + config.
- (constraint) Custom.txt là lớp thủ công độ tin cậy cao, dùng chung với engine cũ; máy không được tự ghi vào file này hay VietPhrase_*.txt.
- (decision) Dùng kiến trúc pipes-and-filters hai pha: LEARN tạo dictionary revision bất biến, TRANSLATE chỉ đọc revision đã freeze.
- (decision) Đường dịch dùng longest-match có precedence và rule pattern; lattice/top-K chỉ phục vụ phân tích ứng viên, không route sang engine khác.
- (decision) Máy sở hữu hai lớp sinh tự động: glossary.auto.tsv theo sách và AutoVietPhrase.txt toàn cục; manual luôn thắng auto.
- (decision) Ứng viên sinh bằng thống kê corpus, heuristic tên riêng và phép ghép kết quả từ các entry VietPhrase hiện có; không sinh target bằng model.
- (decision) Promotion là staged: candidate -> book-auto -> global-auto; global-auto cần bằng chứng nhiều sách và regression corpus.
- (decision) Mỗi promotion tạo revision mới bằng atomic replace; run đang dịch không quan sát thay đổi dictionary giữa chừng.
- (decision) SQLite sở hữu provenance, candidate, revision, evidence, evaluation và rollback; file snapshot là artifact engine nạp.
- (assumption) Ngưỡng tần suất, PMI, entropy, coverage gain và regression budget trong tài liệu là mặc định khởi tạo, phải calibrate bằng raw_china/expect*.txt.
- (decision) Giữ snapshot lossless, checkpoint/resume, cache theo fingerprint, QA cấu trúc và atomic export từ hệ thống hiện tại.
- (decision) Loại router bốn nhánh, model clients, prompt schema, acceptability gate cho model output và cấu hình profile fast/balanced/quality.
- (insight) Reviewer gate phát hiện pin revision vẫn có thể trỏ tới file mutable và global state có thể split-brain giữa book projects.
- (decision) dictionary_revision_id là SHA-256 canonical manifest của toàn bộ layer hash cùng loader/renderer/precedence version; run đọc immutable content-addressed bundle.
- (decision) Resolved dict_dir sở hữu đúng một global registry tại .zhvi-registry; book DB không ghi global auto projection.
- (decision) Publish protocol: temp bundle, fsync, atomic rename, DB insert ready + CAS active pointer, projection reconcile sau commit; không cần atomic transaction xuyên DB/filesystem.
- (decision) Mỗi scope có tối đa một active pointer; book composite revision pin global revision và book/manual layer hashes; global activation không âm thầm đổi active book revision.
- (insight) Reality review: current matcher longest-match before precedence; manual-wins must be scoped to same key/span.
- (decision) Span length wins first; manual precedence compares only same normalized key/span. Override segmentation requires manual entry for the longer key.
- (constraint) Legacy base/human duplicate keys preserve deterministic source/file/load order with diagnostics; only auto same-scope/layer conflict hard-fails.
- (insight) raw_china/expect*.txt chưa phải golden approved; current output diverges substantially and provenance includes legacy AI examples.
- (decision) Global promotion requires approved golden manifest with pinned source/expected hashes and approval metadata; raw expect files remain candidate references until approved.
- (decision) Junk stripping is versioned deterministic preprocessing with source-offset trace; repetition collapse is renderer transformation; QA never mutates.
- (gap) Current runtime remains hybrid and root test collection has undeclared dependency; migration M1 must establish VietPhrase-only baseline before cutover claim.
- (decision) Brownfield cutover is additive/non-destructive: preserve model-era history read-only, invalidate model/prompt/router caches, remove model runtime/deps/config, new runs route only VIETPHRASE.
- (decision) Book-auto hard gates require deterministic single target, no manual conflict, structural QA and affected-block isolation; export requires 100% output-span provenance.
- (event) spine finalized; reviewer gate PASS with no residual findings
