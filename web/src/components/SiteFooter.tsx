export default function SiteFooter() {
  return (
    <footer className="mt-20 pb-10 pt-8 border-t border-[var(--color-border)]">
      <div className="flex flex-col items-center gap-1.5 text-center">
        <p className="text-[11px] tracking-[0.25em] uppercase text-[var(--color-text-dim)]">
          Nghe Truyện
        </p>
        <p className="text-[11px] text-[var(--color-text-dim)]">
          Thư viện đọc &amp; nghe cá nhân — yên tĩnh, không quảng cáo
        </p>
      </div>
    </footer>
  );
}
